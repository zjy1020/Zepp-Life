{
  "name": "zepp-life",
  "version": "1.0.0",
  "description": "Y子步数 - Zepp Life 步数修改工具",
  "private": true,
  "scripts": {
    "test": "node --test",
    "build": "npx cap sync",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0"
  }
}
